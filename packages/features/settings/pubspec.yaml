name: settings
description: settings module
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  go_router: ^16.1.0
  shared_preferences: ^2.5.3

  plugin:
    path: ../../core/plugin
  theme:
    path: ../../core/theme
  widgets:
    path: ../../shared/widgets
  contracts:
    path: ../../core/contracts
  di:
    path: ../../core/di