name: domain
description: domain
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  network_core:
    path: ../../network/core
  action_policy:
    path: ../../core/action_policy

dev_dependencies:
  test: any