name: ui
description: ui
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  localization:
    path: ../localization

dev_dependencies:
  test: any