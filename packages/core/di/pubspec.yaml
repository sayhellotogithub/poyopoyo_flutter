name: di
description: di module
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  contracts:
    path: ../contracts
  plugin:
    path: ../plugin
  flutter_riverpod: ^2.6.1
  go_router: ^16.1.0
