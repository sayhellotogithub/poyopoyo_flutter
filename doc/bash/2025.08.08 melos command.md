```bash

dart pub global activate melos
export PATH="$PATH":"$HOME/.pub-cache/bin"
melos --version
flutter pub get
melos bootstrap
```

#### 安装依赖

melos bootstrap

#### 替换版本占位符

melos run sync_versions

#### 查看模块

melos list

#### 生成相关的文件
melos run build:all
